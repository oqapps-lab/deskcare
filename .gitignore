node_modules/
.expo/
dist/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.local
.env.production

# MCP config with API keys (local-only)
.mcp.json

# Native build outputs (we use Expo Go for now)
ios/
android/
*.xcworkspace
.metro-health-check*

# macOS / Windows
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Russell's raw video drops (per-batch download folders)
drive-download-*/

# MidJourney raw icon outputs — kept locally for re-export, not in repo.
# Resized 1024x1024 versions live in icons/candidates/ and ARE tracked.
icons/u7799383528_*.png

# Local EAS credentials (eas-local-credentials skill). The .p12 + the
# .mobileprovision are already covered by *.p12 / *.mobileprovision globs
# above; explicitly gitignoring credentials.json because it carries the
# .p12 password.
credentials.json
credentials/
